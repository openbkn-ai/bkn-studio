# 知识网络当前差异报告

更新日期：2026-06-06

## 已完成
- 一级菜单「领域知识网络」已由 `knowledge-network` 接管
- 默认入口已切到知识网络模块
- 知识网络列表页整页对齐（V1-1）
- 工作区壳层与概览对齐（V1-2 / V2-1）
- 对象类列表 + 多步创建/编辑 + 详情 + 索引设置（V3-1 主链路）
- **关系类列表 + 两步创建/编辑 + 独立映射页 + 详情 Config 表格（V3-2）**
- **行动类列表 + 两步创建/编辑 + 执行配置 + 详情 Overview/Task + 立即执行（V3-3）**
  - mock 样例：`kn-domain-risk` / `at-risk-block`
  - 工具目录经 `action-type-tool.service` 抽象；真实 `/tools/catalog` 待后端
- **指标计算公式 + 详情/数据查询 Tab（V3-5）**
  - mock 样例：`kn-domain-risk` / `metric-risk-hit-rate`
- 概念分组 / 指标 / 任务基础 CRUD 已落地
- 工作区资源页统一工具栏与列表样式
- **T8 联调验收**：`npm run check`（lint + test + build）通过

## 当前主要差异

### 仍是占位或简化
- 行动类 JSON 导入、调度计划编辑/只读展示（待工具/计划 API）
- 对象类详情数据预览区仍缺
- 指标复杂过滤条件（MultistageFilter 级）仍缺
- 预览仍不是完整 G6 图谱体验（V2-2 暂缓）
- 关系类 JSON 导入未对齐

### 尚未迁入
- 任务深能力（V3-6）

## 下一刀建议

1. **V3-6** 任务深能力
2. **V2-2 预览**（暂缓）
